<Window x:Class="AppPokeAPIViewerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppPokeAPIViewerWPF"
        mc:Ignorable="d"
        Title="PokeAPI Viewer" Height="450" Width="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <ListBox Grid.Column="0"
                     ItemsSource="{Binding ResourceCategoryItems}"
                     SelectedItem="{Binding SelectedResourceCategoryItem, Mode=TwoWay}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding ResourceCategorySelectionChangedCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ListBox>
            
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>

            <ListBox Grid.Column="2"
                     ItemsSource="{Binding EndPointItems, Mode=TwoWay}"
                     SelectedItem="{Binding SelectedEndPointItem, Mode=TwoWay}">
                <ListBox.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenEndPointCommand}"/>
                </ListBox.InputBindings>
            </ListBox>
        </Grid>
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="64"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="開く" Command="{Binding OpenEndPointCommand}"/>
            <Button Grid.Column="2" x:Name="CloseButton" Content="閉じる" Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
